name: Hello world!

on:
  push:
      branches:
        - master

jobs:
    hello_world_job:
      runs-on: ubuntu-latest
      steps:
        - name: hello
          run: echo "Hello world"
        - name: Now it is
          run: date
        - name: Directory content
          run: ls -l
      
    